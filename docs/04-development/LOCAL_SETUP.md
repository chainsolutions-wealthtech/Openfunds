# Installation locale

Référence principale : `README.md`. Prévoir Python compatible avec les workflows, PostgreSQL 16 pour les tests SQL et les dépendances documentées dans `requirements/`. Utiliser des variables d’environnement sans committer de secret. Avant toute commande destructive, employer une base éphémère. Les commandes non exécutées doivent être signalées comme telles.
